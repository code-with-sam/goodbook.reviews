extends layout

block content
  include partials/nav

  include partials/sidebar
  main.gallery__wrapper.clearfix.feed.profile(data-feed-type=`${feed}` data-username=`${name}`)

    section.gallery.clearfix.feed-insert
      .container
